"""Scale x Odyssey — 5-class astronomical image classifier.

Package modules:
    data        leakage-safe file index, origin split, AstroDataset
    transforms  train vs val/test image transforms            (to build)
    model       backbone + classification head                (to build)
    train       training loop                                 (to build)
    eval        metrics, Grad-CAM, OOD evaluation             (to build)
"""

__version__ = "0.1.0"
